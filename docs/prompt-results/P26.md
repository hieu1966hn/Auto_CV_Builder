# P26 - [DOM] Doc luong du lieu trong code hien tai

## Tom tat data flow
- Du lieu vao qua `FormData(form)` trong `refreshPreview()`.
- Validation duoc tinh trong `collectValidationIssues(data)`.
- Render UI tai `renderValidation(issues)` va `updateList(...)`.

## Ket luan cho hoc vien
Luong DOM thuan dang ro: input event -> lay FormData -> xu ly -> cap nhat preview.
